// Counter

// snippet 1:
var counter = 5; // counter reassigns counter to 5.
var rate = 3;
console.log('The total value is ' + String(counter * rate)); // 15, not calling a function.

// 5 * 3.

function counter(count) {
  // ... returns undefined
}


/*
	Arrays are prefect for data of the same type in a list:
*/

let names = ['Alex', 'Joe'];
let numbers = [1, 2, 3];
let days = ['Monday', 'Tuesday', 'Wednesday'];

// adding, removing, retriving, modifying, and iterating

// Objects

/*
	entity with many parts, key / value pairs, different data types, etc.
*/

let person = {
	firstName: 'Joe',
	lastName: 'Montoya',
	gender: 'male',
	age: 30,
	married: false,
}


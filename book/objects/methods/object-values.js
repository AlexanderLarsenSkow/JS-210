let person = { name: 'Bob', age: 30, height: '6ft' };

let personValues = Object.values(person);

console.log(personValues) // ['Bob', 30, '6ft]

// can't predict the order of values in the resulting array.
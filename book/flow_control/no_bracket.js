let x = 4;

if (x === 3)
	console.log('x is 3');
	console.log('x is an odd number.'); // this line runs no matter what. Not part of if statement.
	
	
	// Use brackets or the block will only be one line for the if statement.

	x = 4;
	if (x === 4) {
		console.log('x is 4');
		console.log('x is EVEN baby!')
	}
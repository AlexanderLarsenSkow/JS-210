let a = 1 === 1 ? 'true' : 'false';

let b = 1 === 0 ? 'It is true!' : "It is not true."

console.log(a)
console.log(b)

// Useful because you can assign it to a variable or reuse it later. Can't do that with a normal
// if statement.

// Other Uses:

// let foo = hitchhiker ? 42 : 3.1415;
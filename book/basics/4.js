console.log('5' + 10);

// This code logs the string '510' because the object on the left side of the + operator is a string.
// In this case, it coerces the Number 10 into a string object, which is then concactentated to the 
// string character 5.
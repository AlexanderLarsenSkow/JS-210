let arr = [1, 2, 3, 4];
console.log(arr.includes(4)); 

// returns true or false depending on if hte argument is inside the array.
// includes uses === so we can't check for the existence of an object here. Love JS so much. 

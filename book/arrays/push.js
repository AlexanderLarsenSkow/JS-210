let array = [1, 2, 3, 4, 5];

array.push(6);

console.log(array) // [1, 2, 3, 4, 5, 6]

// push is just like in Ruby. Adds argument to the end of the array.
// push returns the new length of the array.
let a = [10, 5, 8, 3, 5, 6,];
console.log(a.sort()); // sort doesn't work on numbers that is crazy.

let b = ['e', 'a', 'c', 'b'];
b.sort();

console.log(b);

// sort() is destructive lol. Who wrote this language my lord.
// also doesn't work with numbers? What's that about?